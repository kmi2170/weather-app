export type LocationType = {
  // city: string;
  // region: string;
  // timezone: string;
  // postal: string;
  // country_code: string;
  // country_name: string;
  // lat: number;
  // lon: number;
};

export type QueryType = {
  // city: string;
  // state?: string;
  // units?: 'imperial|metric';
  // lang?: 'en|ja';
};
